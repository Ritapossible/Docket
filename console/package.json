{
  "name": "@docket/console",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "node scripts/gen-threats.mjs",
    "dev": "npm run gen && vite",
    "build": "npm run gen && tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "npm run gen && tsc -b --noEmit false --emitDeclarationOnly false"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
