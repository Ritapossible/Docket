{
  "name": "@docket/sdk",
  "version": "0.1.0",
  "description": "Client for Docket mandates. Turns a policy refusal into a thrown error, because the contract does not revert on one.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "viem": "^2.21.0"
  },
  "license": "MIT"
}
